# Example Post-Stage for a device
post-stage:
  - cmd: newdutctl
    name: Power off DUT
    parameters:
      agent: "[[attributes.Agent]]"
      device: "[[attributes.Device]]"
      command: power
      args: ["off"]
